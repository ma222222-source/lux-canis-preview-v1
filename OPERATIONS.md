# Lux Canis 運用・引き継ぎメモ

## 運営者向けURL

- お客様向け: https://lux-canis-preview-v1.pages.dev/
- 管理者専用の入口: https://lux-canis-preview-v1.pages.dev/admin.html
- 公開ページに管理者リンクは置かない。管理者入口はブックマークで管理する。
- URLは秘密情報ではない。検索対象外の指定も認証の代わりにはならない。

## 会員機能の実態

| 項目 | 保存先・動作 |
| --- | --- |
| 会員名・メール・同意記録 | Cloudflare D1。管理者は確認可能 |
| パスワード | ランダムsalt付きPBKDF2-SHA256、100,000回。平文は保存・返却しない |
| ログイン | ランダムなトークンをHttpOnly/Secure/SameSite=Strict Cookieで保持。DBにはトークンのハッシュを保存 |
| 会員セッション | 最長30日。削除・全端末ログアウト・パスワード変更時に失効 |
| 管理者セッション | 最長8時間。管理者秘密値を変更すると旧セッションも無効化 |
| 再販希望 | D1に商品・カラーと会員IDを保存。別端末で参照・解除可能 |
| 通知設定 | D1。会員ページ内のお知らせを種類で絞る。重要情報は常時表示 |
| いいね | 各ブラウザのローカル保存。アカウント間同期はしない |
| 問い合わせ | D1に保存。運営者が確認し、手動で返信する。自動返信はない |
| 商品画像 | Cloudflare R2。静的なブランド画像はGit管理 |

## 今回の安全対策

- 管理画面への公開リンクを削除。`/admin`と`/admin.html`にno-store/noindex指定。
- パスワード変更は現在のパスワードで再認証し、新しいパスワードは15文字以上。
- 全端末ログアウトとパスワード変更は認証の世代番号を更新。変更前に始まったログイン処理から発行されたセッションも使用できない。
- 並行したパスワード変更で上書き競合した場合は再ログインを求める。
- 同じIPの会員ログインは60回、管理者ログインは10回、新規登録は5回、問い合わせは6回まで（15分区切り）。
- 会員メール単位のログインは12回、会員ID単位のパスワード変更・退会確認は各8回まで（15分区切り）。
- 制限判定はD1の単一更新。キーにはハッシュ値を用い、IP・メールそのものは制限表に保存しない。ただしハッシュは完全な匿名化を意味しない。
- 期限を過ぎた試行記録は対象リクエスト時、期限切れセッションはログイン時に最大64件ずつ除去する。無アクセス中は残る。
- JSONの入力上限は64 KiB。入力形式の異常は400、過大な入力は413、試行制限は429と待ち時間を返す。
- 未接続のメール通知はUIから除外し、APIでの有効化も拒否する。接続後も自動で受信同意にしない。
- 管理者画面に本運用前の不足を表示し、未解決なのに「公開準備完了」とは表示しない。

## 本運用に進めない条件（未解決）

1. 管理者パスワードが推測可能な状態。2026-09-10時点、所有者の指示により変更していない。会員情報の閲覧権限を守るには、強い秘密値または追加のアクセス認証が必須。
2. メールの本人確認・忘れたパスワードの自動復旧が未実装。登録アドレスは本人確認済みとして扱わない。メールアドレスが分かるだけで第三者へアカウントを渡したり、運営者が本人確認なしに再設定してはいけない。
3. メール配信・自動返信は未接続。サイト内のお知らせの表示と、メール・スマホへのプッシュ配信は別機能。
4. 本番データの定期退避・復元訓練、R2画像の独立したバックアップは未設定。GitHubだけでは会員情報・投稿内容・アップロード画像は復旧できない。
5. 価格・商品情報・実際のレビュー・問い合わせ返信体制は運営者による確定が必要。
6. 個人情報保持期間、本人確認手順、障害・漏えい時の連絡担当を確定する必要がある。

この実装は独立した専門機関の侵入テストを受けたものではない。「恒久的に安全」「バグがない」という保証ではなく、更新と点検が必要。

## 無料範囲のメール接続方針

送信サービスの無料枠は利用候補になるが、送信元の本人確認が必要。所有していないドメインを送信元にしない。ブランド用独自ドメインの新規取得は通常有料のため、無料作業の権限だけでは購入しない。

無料から検討する場合はBrevoなどで所有者がアカウント・送信元を認証する。承認・送信制限があり、無料メールアドレスは送信元が置き換わる場合もある。ブランドの正式運用では独自ドメインからの認証済み送信を推奨する。

接続が決まった後に、期限付き・一度だけ使える確認/復旧トークン（DBにはハッシュのみ）、再送制限、送信失敗処理、配信テストを実装する。トークンをログへ出さず、復旧リクエストでは登録の有無を明かさない。新規の外部サービス・顧客宛てメール送信は承認を得てから行う。

## 更新手順（担当者向け）

1. ローカルの変更内容と本番の状態を確認する。秘密値やDBバックアップはGitに入れない。
2. `npx wrangler d1 migrations apply lux-canis-data --local`
3. `node scripts/build-static.mjs`
4. `npx wrangler pages dev dist --port 8795`
5. PowerShellで `$env:BASE_URL='http://127.0.0.1:8795'` とし、`node tests/smoke.mjs`、`node tests/account-security.mjs`、`node tests/frontend-regressions.mjs` を実行する。データ変更を伴うテストはlocalhost以外では拒否される。
6. スマホ・PCでトップ、登録/ログイン、安全設定、商品、問い合わせ、管理入口を確認する。
7. 本番へ出す承認を得て、復旧用バックアップ・復元ポイントを確認する。
8. 本番DBへ未適用の追加マイグレーションだけ適用し、GitHubへpush、Cloudflare Pagesへdistをデプロイする。今回の0005適用で旧管理者セッションは無効になる。
9. 公開URLと管理URL、CSS/JSの版、未認証APIの拒否を確認する。本番でダミー会員や問い合わせを作って試さない。

## バックアップと復旧

- D1のTime TravelはWorkers Freeで過去7日、Paidで30日の復元が公式に案内されている。実際の契約プラン・保存状態は運用時に確認する。
- 長期保存には日付付きのDB書き出しが必要。バックアップには個人情報・認証ハッシュを含むため、GitHub・公開dist・公開共有リンクへ絶対に置かない。
- 誤更新時は本番DBを即上書きせず、隔離したローカルDBへ復元して件数・内容を確認してから復旧方針を決める。
- 2026-09-10にローカルDBを書き出し、別のローカル保存先へ復元する訓練を実施。本番からの復元、R2画像の復元は未検証。
- コードのロールバックとDBのロールバックは別操作。今回の認証世代を知らない旧APIへ安易に戻すとセッション失効の保証が失われるため、認証修正を維持した前方修正を優先する。

## 参照

- [OWASP: Authentication](https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html)
- [OWASP: Forgot Password](https://cheatsheetseries.owasp.org/cheatsheets/Forgot_Password_Cheat_Sheet.html)
- [Cloudflare: Time Travel](https://developers.cloudflare.com/d1/reference/time-travel/)
- [Brevo: 送信元要件](https://help.brevo.com/hc/en-us/articles/14925263522578-Comply-with-Gmail-Yahoo-and-Microsoft-s-requirements-for-email-senders)
