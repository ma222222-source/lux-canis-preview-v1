# 2026-09-10 公開版の改善記録

前回の文字の影・改行・ロゴリンク・割引表記の修正に加え、以下を改善しました。

1. 問い合わせのEnter操作でも確認画面を経由し、確認前に送信しない。
2. 空白だけの入力を拒否し、最初の不備へフォーカスする。
3. 入力エラーを各入力欄と読み上げ用に関連付け、修正するとエラーを解除する。
4. 入力・確認・完了の現在位置を明示し、画面を切り替えたら内容へフォーカスする。
5. 問い合わせ送信中の連打・戻る操作を防ぎ、失敗時は入力を保持する。
6. 会員登録・ログインの処理中は方式の切り替えと重複送信を防ぐ。
7. 認証完了後はパスワード欄を非表示形式に戻す。
8. ログアウトが通信エラーになった場合に成功表示しない。
9. 再販希望からログインした後は、元の商品・選択カラーへ戻る。戻り先は同じサイトの商品ページだけに限定する。
10. お知らせの取得失敗がログイン情報の表示まで止めないようにする。お知らせには再読み込みを用意する。
11. 再販希望の登録中は二重操作とカラー変更を防ぐ。
12. 再販待ちの解除中は処理中表示にし、重複操作を防ぐ。
13. 商品詳細の写真は全体が見えるように表示し、切り取らない。
14. 商品写真を読み込めない場合は案内と再読み込みを表示する。
15. 長い商品名・連続した文字・メールアドレスによる横はみ出しを防ぐ。
16. 会員登録のプライバシーリンクと再販解除ボタンの操作領域を44px以上にする。
17. 問い合わせ確認画面の見出しを読みやすいサイズ・行間に整える。

## 確認

- JavaScript構文、本番用ビルド、既存APIスモークテストを確認。
- tests/frontend-regressions.mjs で通信失敗・二重送信・登録方式の切り替え・戻り先制限・写真読込失敗を検証。
- ローカルのトップ・会員・商品詳細・問い合わせ画面で375px、812px、1440px幅の横はみ出しがないことを確認。
- 問い合わせのエラー表示、確認画面へのフォーカス、入力保持をブラウザで確認。ブラウザから問い合わせは送信していない。
- 商品カラーBlueをURLから復元でき、写真が全体表示になることを確認。
- 変更した配信用HTML/CSS/JSのgzip合計は、作業開始時のHEAD比で約1.8KB増。新しい画像・フォント・外部ライブラリは追加していない。実機通信での速度スコアの保証ではない。
- テスト用スクリプトとこの記録は公開用distには含めない。

## 残る制約

- メール自動配信は未接続。
- Instagramの実URL、正式価格、実レビューは未提供部分を維持。
- 管理者パスワード0000は推測されやすく、本運用前に変更が必要。今回変更していない。
